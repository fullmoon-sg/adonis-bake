@layout('base')
@section('content')
<form method="POST">
  {{csrfField()}}

    <tr>
      <th scope="col">No.</th>
      <th scope="col">Username</th>
      <th scope="col">Email</th>
      <th scope="col">Created Date</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row">{{account.id}}</th>
      <td>{{account.username}}</td>
      <td>{{account.email}}</td>
      <td>{{account.created_at}}</td>
    </tr>
  </tbody>
  <div class= "alert alert-danger">
              Confirm you want to delete ?
              <input type="submit" class="btn btn-danger mr-3" value="Yes"/>
               <a href="{{route('display_all_accounts')}}" class="btn btn-success">Back</a>
             </div>
          </div>
</form>
  @endsection
