@layout('base')
@section('content')

<div style=" display: flex; flex-direction: column">
<table class="table" style="width: 1200px">
  <thead>
    <tr>
      <th scope="col">No.</th>
      <th scope="col">Username</th>
      <th scope="col">Created Date</th>
      <th scope="col">Action</th>
    </tr>
  </thead>
  <tbody>
     @each(account in accounts)
    <tr>
      <th scope="row">{{account.id}}</th>
      <td>{{account.username}}</td>
      <td>{{account.created_at}}</td>
     <td>
       <a href="{{route('delete_adonis_account', {id:account.id})}}" class="btn btn-danger mt-2 mb-2">Delete Account</a>
    </td>
    </tr>
     @endeach
  </tbody>
</table>
<div>
    <a href="{{route('adminRegister')}}" class="btn btn-success mt-2 mb-2">Create New Account</a>
</div>
</div>
@endsection
